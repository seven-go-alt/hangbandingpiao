# PR 描述：Vue 3 前端重构与后台只读管理端 MVP

## 背景

本 PR 在保留现有 Java Spring MVC 后端接口的前提下，引入独立 Vue 3 前端子项目 `web/`，用于逐步替换原 JSP + LayUI + jQuery + 页面内 Vue 的混合前端。

本次改造目标不是重写后端，而是先完成前端工程化、用户端主流程和后台管理端只读 MVP，降低后续维护成本。

## 主要变更

### 1. 新增 Vue 3 前端工程

新增独立前端子项目：

```text
web/
```

技术栈：

- Vue 3
- Vite
- TypeScript
- Vue Router
- Pinia
- Axios
- Element Plus（按需使用）

工程能力：

- 路由懒加载
- Axios 请求封装
- API 响应统一解包
- Pinia 登录态管理
- vendor chunk 拆分
- Element Plus 去全量注册，降低前端包体积

---

### 2. 用户端 Vue MVP

已完成用户端主要页面与流程：

- 首页
- 登录 / 注册
- 航班列表 / 航班详情
- 新闻列表 / 新闻详情
- 订单创建 / 订单列表 / 订单详情
- 收藏列表 / 取消收藏 / 跳转航班详情
- 留言列表 / 提交留言 / 按航班过滤
- 个人中心 / 资料编辑
- 充值说明页
- 余额感知下单流程
- 下单成功后跳转订单详情
- 首页登录后余额导流
- 航班列表余额提醒

说明：后端当前没有独立充值 API，因此充值页只做说明，不伪造前端充值能力。

---

### 3. 后台管理端 Vue MVP

新增后台管理端 Vue 页面：

- 后台布局与导航
- 仪表盘统计
- 最近订单
- 用户管理列表
- 航班管理列表
- 订单管理列表
- 后台基础登录守卫

后台用户管理支持：

- 账号 / 姓名 / 手机号 / 身份证号 / 性别筛选
- 分页
- 每页条数切换
- 用户详情弹层
- 头像、余额、创建时间等信息展示

后台航班管理支持：

- 航班号 / 始发站 / 目的地 / 航班类型 / 价格区间筛选
- 分页
- 每页条数切换
- 航班详情弹层
- 航班图片与富文本详情展示

后台订单管理支持：

- 订单号 / 航班号 / 乘客姓名 / 手机号 / 状态筛选
- 分页
- 每页条数切换
- 订单详情弹层
- 订单状态展示
- 乘客与航班信息展示

---

### 4. 后台安全边界

当前后台 Vue 页面刻意保持只读能力：

- 只接入分页查询接口
- 只接入详情查询接口
- 暂不接新增、编辑、删除、退款、改状态等写操作

原因：后台写操作会直接影响业务数据，需要后续逐个确认接口语义、权限边界和表单校验后再开放。

当前后台路由已加基础 token 守卫；角色级管理员权限还需要结合后端登录/角色方案进一步确认。

---

### 5. 文档补充

新增 / 更新：

- `MERGE_REQUEST.md`
- `README.md`
- `web/README.md`
- `task_plan.md`
- `findings.md`
- `progress.md`

用于记录：

- 当前重构范围
- 合并顺序
- 测试清单
- 风险边界
- 后续建议

## 验证

已在 `web/` 下执行：

```bash
npm run build
```

结果：构建成功。

已知非阻塞警告：

```text
INVALID_ANNOTATION from node_modules/@vueuse/core/dist/index.js
```

该警告来自依赖包中的 pure annotation 注释位置，不影响当前构建产物生成。

## 建议测试清单

### 构建

- [ ] `cd web && npm install`
- [ ] `cd web && npm run build`

### 用户端冒烟

- [ ] 首页可打开
- [ ] 登录 / 注册页面可打开
- [ ] 航班列表可查询
- [ ] 航班详情可打开
- [ ] 新闻列表 / 新闻详情可打开
- [ ] 登录后可进入订单列表
- [ ] 可从航班详情进入下单页
- [ ] 余额不足下单有提示
- [ ] 可进入收藏列表
- [ ] 可进入留言列表并提交留言
- [ ] 可进入个人中心并编辑资料

### 后台端冒烟

- [ ] 未登录访问 `/admin` 会跳登录
- [ ] 登录后 `/admin` 可打开仪表盘
- [ ] `/admin/users` 可筛选、分页、查看详情
- [ ] `/admin/flights` 可筛选、分页、查看详情
- [ ] `/admin/orders` 可筛选、分页、查看详情

## 合并说明

如果这是第二个 PR，请先确保基础分支已经合并：

1. `feat/frontend-cleanup` → `main`
2. 当前 `develop` → `main`

建议使用：

- `Create a merge commit`

这样能保留阶段性重构边界。

## 风险与后续

- Vue 前端依赖现有 Java 后端接口返回结构，后端接口若变化需要同步调整 API 层。
- 后端没有独立充值 API，因此当前充值页为说明页。
- 后台写操作暂未开放，后续建议按模块逐步确认后实现。
- 后台角色级权限仍需结合后端登录方案继续细化。

## 后续建议

本 PR 合并后，建议下一阶段再拆独立 PR：

1. 后台新闻管理 Vue 化
2. 后台留言管理 Vue 化
3. 后台收藏管理 Vue 化
4. 后台字典管理 Vue 化
5. 后台写操作按接口语义逐步开放
